# 房东俩 · 产品设计文档 v1.0

> 一份给"房东俩"项目的正式产品设计文档。
> 后续所有开发工作都基于本文档展开。
>
> 版本：v1.0 ｜ 日期：2026年5月 ｜ 状态：设计阶段

---

## 目录

1. 产品定位与价值主张
2. 目标市场与用户
3. 商业模式
4. MVP功能清单
5. 完整数据模型
6. 关键算法说明
7. AI功能技术方案
8. 技术架构与选型
9. 风险与待验证假设
10. 开发路线图

---

## 一、产品定位与价值主张

### 1.1 一句话定位

**"AI驱动的智能房东助手——让管理几套房，像养一盆花一样轻松。"**

### 1.2 核心价值主张

我们不是给房东做一个"账本"，而是做一个"会动手的助手"：

- **AI帮你做事**：截图自动识别、账单自动生成、刁难话术自动建议
- **协同不必费心**：单人也好用，可邀请家人共管
- **一切信息留痕**：合同、对话、收款记录、维修历史，永远可追溯

### 1.3 与竞品的差异化

现有竞品（巴乐兔房东、水滴管家、各类公寓SaaS）的共同特点：
- 偏中介/二房东视角，对个人房东太重
- 纯CRUD工具，需要房东自己点点点
- 协作功能薄弱，主要是单人记账
- 没有AI能力，跟不上时代

我们的差异：**真正"为个人房东"+ "AI原生" + "协同友好"**

---

## 二、目标市场与用户

### 2.1 主战场：中国大陆

为什么选大陆：
- 个人房东数量庞大（保守估计千万级）
- 微信生态深度整合（账单分享、租客沟通天然在微信）
- 缺乏真正好用的个人房东工具（市场空缺）
- 你和团队对本土场景理解最深

### 2.2 货币与本地化

- 货币：人民币 (¥)
- 身份验证：身份证号格式（18位）
- 支付场景：微信、支付宝、银行转账
- 时间：北京时间（UTC+8）
- 法律：参考《住房租赁条例》《民法典合同编》

### 2.3 目标用户类型（粗略，画像后续详做）

- **小微房东**（1-3套）：免费版主力，传播种子用户
- **中型房东**（4-10套）：核心付费用户群
- **专业房东**（10套以上）：高ARPU用户，但数量少
- **夫妻/家庭共管**：差异化亮点，跨越上面所有类型

---

## 三、商业模式

### 3.1 模式概览：基础免费 + AI按次 + 增值订阅

```
┌─────────────────────────────────────┐
│  基础版（免费）                      │
│  最多3套房 ｜ 5次AI/月 ｜ 单人      │
│  目标：广覆盖，建立口碑              │
├─────────────────────────────────────┤
│  专业版（¥19/月 或 ¥168/年）        │
│  房源不限 ｜ 100次AI/月 ｜ 3人协同  │
│  目标：核心收入来源                  │
├─────────────────────────────────────┤
│  AI加油包（独立购买）                │
│  100次¥9.9 ｜ 500次¥39.9            │
│  目标：高用量用户的额外收入          │
└─────────────────────────────────────┘
```

### 3.2 关键定价逻辑

| 决策点 | 数值 | 设计逻辑 |
|---|---|---|
| 免费版房源上限 | 3套 | 覆盖80%个人房东，3套以下用户付费意愿低 |
| 免费版AI次数 | 5次/月 | 让用户体验AI价值，但不够日常使用 |
| 专业版月费 | ¥19 | 视频会员价格锚点，用户决策无压力 |
| 专业版AI次数 | 100次/月 | 远超日常需求，制造"用不完"心智 |
| 加油包 | ¥9.9/100次 | 每次¥0.099，比订阅便宜，但促进多用 |

### 3.3 成本估算（每月每用户）

- 基础设施（Supabase + Vercel + 推送）：≈¥2
- AI调用成本（按次约¥0.05-0.10）：基础版≤¥0.5，专业版≤¥10
- 客服与运营：早期忽略，后期摊销

**专业版毛利率约 50%-60%**，健康可持续。

### 3.4 商业化时机

- **V1.0 MVP**：不开通付费，全部免费，专注产品打磨
- **V1.5（用户达500-1000）**：开通付费墙，验证转化
- **V2.0（用户达5000+）**：增值功能、企业版

---

## 四、MVP 功能清单（V1.0）

### 4.1 设计原则

1. **能不做就不做**：第一版越精简越好
2. **AI是骨架不是装饰**：架构为AI预留位置
3. **本地化优先**：紧扣中国大陆场景
4. **协同是可选项**：默认单人，可邀请

### 4.2 V1.0 必做功能

#### 模块A：账户与协同

- 手机号注册登录（验证码）
- 创建"家庭组"
- 邀请配偶/家人加入（生成邀请链接）
- 操作日志（谁在什么时候做了什么）

#### 模块B：房源管理

- 添加/编辑/删除房源
- 房源信息：名称、地址、户型、面积、状态
- 房源照片（最多5张）
- 状态切换：出租中/空置/装修中

#### 模块C：租客与租约

- 添加/编辑租客（姓名、电话、身份证、紧急联系人）
- 租客复用（一人可关联多次租约）
- 创建租约：选房源、选租客、租期、租金、押金、付款周期
- 合租支持（一份租约多个租客）
- 合同附件上传（PDF或图片）

#### 模块D：账单与收款

- 自动按规则生成下月账单
- 首月/末月按比例计算（按天）
- 多种生成模式可选（自然月/整月顺延）
- 标记已收（手动）
- 支持部分付款（一条账单多次收款）
- 上传打款凭证截图

#### 模块E：AI识别打款截图 ⭐

- 拍照或上传截图
- AI自动识别金额、时间、付款人
- 匹配到对应账单
- 用户确认后标记已收
- 错误情况支持手动修正

#### 模块F：水电杂费

- 手动录入抄表读数（V2再做AI识别）
- 单价设置（含公摊算法）
- 自动计算费用，并入下月租金账单

#### 模块G：提醒中心

- 租金到期前3天 / 当天 / 逾期1天 / 逾期7天
- 合同到期前30天 / 7天
- 抄表日提醒
- 应用内+手机推送

#### 模块H：财务概览

- 本月：应收/已收/未收
- 各房源独立小计
- 年度总收入（简单数字，图表V2做）

### 4.3 V1.0 不做（V2及以后再加）

| 功能 | 推迟原因 |
|---|---|
| 租客自助端 | 验证未明，先看房东端能否站住 |
| 抄表AI识别 | 复杂度高，与"打款识别"重复 |
| 维修工单 | 频率低，先用备注代替 |
| 财务图表 | 第一年数据不足 |
| 电子合同 | 涉及法律资质 |
| 智能催租话术 | AI能力第二批再做 |
| 涨租建议 | 需积累周边租金数据 |
| 数据导出 | V1.5加上 |

---

## 五、完整数据模型

### 5.1 表结构总览

共 **12 张表**，分为四组：

```
身份与权限组    身份基础
├ users
├ households
└ household_members

核心业务组      业务对象
├ properties
├ tenants
├ leases
└ lease_tenants

业务流水组      业务记录
├ bills
├ payments
└ meter_readings

辅助记录组      辅助功能
├ reminders
├ activity_logs
└ attachments
```

### 5.2 详细表结构

下面所有表都默认包含 `created_at` 和 `updated_at` 字段，不再单独列出。

#### users 用户表

```
id              uuid          主键
phone           varchar(20)   手机号，唯一
name            varchar(50)   姓名
avatar_url      text          头像URL
email           varchar(100)  邮箱（可选）
last_login_at   timestamp     最后登录时间
status          varchar(20)   active / disabled
```

#### households 家庭组表

```
id              uuid          主键
name            varchar(50)   家庭名（默认"我的家庭"）
owner_user_id   uuid          创建者ID
plan            varchar(20)   free / pro
plan_expires_at timestamp     付费到期时间
ai_quota_left   int           本月剩余AI次数
ai_quota_reset_at timestamp   配额重置时间
```

#### household_members 家庭成员表

```
id              uuid          主键
household_id    uuid          家庭ID
user_id         uuid          用户ID
role            varchar(20)   owner / member
joined_at       timestamp     加入时间
```

#### properties 房源表

```
id              uuid          主键
household_id    uuid          所属家庭
name            varchar(100)  房源昵称（"滨江花园3-501"）
full_address    text          完整地址
city            varchar(50)   城市
district        varchar(50)   区
type            varchar(20)   一居/两居/三居/合租房间
area            decimal(6,2)  面积平方米
status          varchar(20)   rented / vacant / renovating
photos          jsonb         照片URL数组
notes           text          备注
created_by      uuid          创建人
```

#### tenants 租客表

```
id                  uuid          主键
household_id        uuid          所属家庭
name                varchar(50)   姓名
phone               varchar(20)   电话
id_type             varchar(20)   id_card / passport
id_number           varchar(50)   证件号（加密存储）
emergency_name      varchar(50)   紧急联系人姓名
emergency_phone     varchar(20)   紧急联系人电话
notes               text          备注（性格、职业等）
tags                jsonb         标签（["守信","养宠物"]）
created_by          uuid          创建人
```

#### leases 租约表

```
id                  uuid          主键
property_id         uuid          房源ID
start_date          date          起始日期
end_date            date          结束日期
rent_amount         decimal(10,2) 月租金
deposit_amount      decimal(10,2) 押金
payment_cycle       varchar(20)   month / quarter / half_year / year
billing_day         int           每月几号生成账单（默认1）
billing_mode        varchar(20)   natural_month / rolling_month
status              varchar(20)   active / expired / terminated
contract_url        text          合同附件URL
notes               text          备注
created_by          uuid          创建人
```

#### lease_tenants 租约-租客关联表（处理合租）

```
id              uuid          主键
lease_id        uuid          租约ID
tenant_id       uuid          租客ID
is_primary      bool          是否主租客
share_ratio     decimal(5,2)  分摊比例（合租场景）
```

#### bills 账单表

```
id                  uuid          主键
lease_id            uuid          租约ID
period_start        date          账期起
period_end          date          账期止
days_count          int           本期天数
proration_factor    decimal(5,4)  比例系数（不足月时<1）
due_date            date          到期日
rent_amount         decimal(10,2) 租金部分
utility_amount      decimal(10,2) 水电部分
other_amount        decimal(10,2) 其他费用
total_amount        decimal(10,2) 应收总额
paid_amount         decimal(10,2) 已收总额（自动计算）
status              varchar(20)   pending / partial / paid / overdue
notes               text          备注
```

#### payments 收款记录表

```
id                  uuid          主键
bill_id             uuid          所属账单
amount              decimal(10,2) 金额
paid_at             timestamp     收款时间
method              varchar(20)   wechat / alipay / bank / cash
proof_url           text          凭证URL
ai_recognized       bool          是否AI识别生成
ai_confidence       decimal(3,2)  AI置信度（0-1）
notes               text          备注
created_by          uuid          创建人
```

#### meter_readings 抄表记录表

```
id                  uuid          主键
property_id         uuid          房源ID
meter_type          varchar(20)   electricity / water / gas
reading_date        date          抄表日
last_reading        decimal(10,2) 上次读数
current_reading     decimal(10,2) 本次读数
usage               decimal(10,2) 本期用量（自动计算）
unit_price          decimal(10,4) 单价
amount              decimal(10,2) 费用（自动计算）
shared_ratio        decimal(5,2)  公摊比例
notes               text          备注
created_by          uuid          抄表人
```

#### reminders 提醒表

```
id                  uuid          主键
household_id        uuid          所属家庭
type                varchar(30)   rent_due / lease_expire / meter_reading / custom
title               varchar(200)  提醒标题
description         text          详细说明
related_type        varchar(20)   bill / lease / property
related_id          uuid          关联记录ID
trigger_at          timestamp     触发时间
status              varchar(20)   pending / sent / read / dismissed
sent_at             timestamp     发送时间
```

#### activity_logs 操作日志表

```
id              uuid          主键
household_id    uuid          所属家庭
user_id         uuid          操作人
action          varchar(50)   create_property / mark_paid / 等
target_type     varchar(30)   property / lease / bill 等
target_id       uuid          目标对象ID
target_name     varchar(200)  对象名称（冗余存储，便于显示）
metadata        jsonb         附加信息
created_at      timestamp     发生时间
```

#### attachments 附件表

```
id              uuid          主键
household_id    uuid          所属家庭
related_type    varchar(30)   property / lease / payment / tenant
related_id      uuid          关联ID
file_name       varchar(200)  文件名
file_url        text          文件URL
file_size       int           大小（字节）
mime_type       varchar(50)   类型
uploaded_by     uuid          上传人
```

### 5.3 数据关系图

```
households
   │
   ├──< household_members >── users
   │
   ├──< properties
   │       │
   │       ├──< meter_readings
   │       │
   │       └──< leases
   │              │
   │              ├──< lease_tenants >── tenants
   │              │
   │              └──< bills
   │                     │
   │                     └──< payments
   │
   ├──< reminders
   ├──< activity_logs
   └──< attachments
```

### 5.4 关键索引

为保证性能，需要创建以下索引：

```sql
-- 用户登录
CREATE INDEX idx_users_phone ON users(phone);

-- 数据隔离：每个查询必须按 household_id 过滤
CREATE INDEX idx_properties_household ON properties(household_id);
CREATE INDEX idx_tenants_household ON tenants(household_id);

-- 业务查询
CREATE INDEX idx_leases_property_status ON leases(property_id, status);
CREATE INDEX idx_bills_lease_period ON bills(lease_id, period_start);
CREATE INDEX idx_bills_status_due ON bills(status, due_date);
CREATE INDEX idx_payments_bill ON payments(bill_id);

-- 提醒查询
CREATE INDEX idx_reminders_trigger ON reminders(household_id, trigger_at, status);

-- 日志
CREATE INDEX idx_logs_household_time ON activity_logs(household_id, created_at DESC);
```

### 5.5 数据隔离与安全

**核心原则**：每个家庭组的数据严格隔离，互不可见。

实现方式：
- 所有业务表都包含 `household_id` 字段
- 用 Supabase 的 Row Level Security (RLS) 强制隔离
- 用户只能访问自己所在家庭组的数据
- 敏感字段（身份证号）加密存储

---

## 六、关键算法说明

### 6.1 账单按比例计算

#### 场景定义

租约：4月5日 - 8月5日
固定收租日：每月1号
月租金：¥5,000

#### 自然月模式（推荐默认）

按"账期天数 / 当月总天数"计算比例。

| 账单 | 账期 | 天数 | 计算 | 应收 |
|---|---|---|---|---|
| 1 | 4月5日-4月30日 | 26天 | 5000 × 26/30 | ¥4,333.33 |
| 2 | 5月1日-5月31日 | 31天 | 5000 | ¥5,000.00 |
| 3 | 6月1日-6月30日 | 30天 | 5000 | ¥5,000.00 |
| 4 | 7月1日-7月31日 | 31天 | 5000 | ¥5,000.00 |
| 5 | 8月1日-8月5日 | 5天 | 5000 × 5/31 | ¥806.45 |

**总计**：¥20,139.78（接近但不等于4个月¥20,000，差额来自不同月份天数）

#### 整月顺延模式（可选）

不与日历月对齐，按"租期月"计算。

| 账单 | 账期 | 应收 |
|---|---|---|
| 1 | 4月5日-5月4日 | ¥5,000 |
| 2 | 5月5日-6月4日 | ¥5,000 |
| 3 | 6月5日-7月4日 | ¥5,000 |
| 4 | 7月5日-8月4日 | ¥5,000 |
| 5 | 8月5日-8月5日 | ¥161.29 |

**实现伪代码**：

```typescript
function generateBills(lease: Lease): Bill[] {
  const bills: Bill[] = [];
  const { start_date, end_date, rent_amount, billing_day, billing_mode } = lease;

  if (billing_mode === 'natural_month') {
    // 按自然月，首末按比例
    let current = start_date;
    while (current <= end_date) {
      const monthEnd = endOfMonth(current);
      const periodEnd = monthEnd > end_date ? end_date : monthEnd;
      const periodStart = current;
      const daysInPeriod = daysBetween(periodStart, periodEnd);
      const daysInMonth = getDaysInMonth(current);
      const factor = daysInPeriod / daysInMonth;
      const amount = round(rent_amount * factor, 2);

      bills.push({ period_start: periodStart, period_end: periodEnd, amount });

      current = startOfNextMonth(current);
    }
  } else {
    // rolling_month: 按租期月顺延
    // ...类似逻辑
  }

  return bills;
}
```

### 6.2 部分付款状态计算

```
status = 
  if paid_amount == 0 then 'pending'
  if paid_amount < total_amount then 'partial'
  if paid_amount >= total_amount then 'paid'

if due_date < today AND paid_amount < total_amount then 'overdue'
```

### 6.3 水电费分摊（合租）

```
每个租客应付费 = 总费用 × 该租客分摊比例
```

合租租客的 `share_ratio` 在 `lease_tenants` 中预设，默认平均分摊。

### 6.4 AI配额管理

```
每月1号自动重置：household.ai_quota_left = plan_default_quota
每次AI调用：ai_quota_left -= 1（必须 > 0 才能调用）
购买加油包：ai_quota_left += 加油包数量
```

---

## 七、AI 功能技术方案

### 7.1 V1.0 唯一AI功能：打款截图识别

#### 输入

- 用户上传一张图片（微信/支付宝/银行APP的打款截图）
- 当前租约和未支付账单列表（系统自动提供）

#### 期望输出

- 识别金额（必需）
- 识别付款时间（必需）
- 识别付款方姓名（可选，用于核对）
- 识别付款方式（微信/支付宝等）
- 匹配最可能对应的账单
- 给出置信度评分

#### 技术方案

使用多模态大模型（如 Claude/GPT-4o）做图像理解。

**Prompt 模板**（伪代码）：

```
你是一个财务识别助手。请从这张打款截图中识别以下信息：
1. 转账金额（人民币）
2. 转账时间
3. 付款方姓名
4. 收款方姓名
5. 付款渠道（微信/支付宝/银行）

如果无法识别，返回 null。

输出格式：JSON
{
  "amount": 数字,
  "paid_at": "ISO时间",
  "from_name": "字符串",
  "to_name": "字符串",
  "method": "wechat|alipay|bank|other",
  "confidence": 0-1之间的小数
}
```

收到结果后，系统按以下规则匹配账单：

```
匹配优先级：
1. 金额完全相等的未支付账单
2. 金额相等的部分支付账单（差额匹配）
3. 金额接近的（±10%）
```

#### 容错设计

- AI识别错误时，用户可手动修正所有字段
- 置信度<0.8时主动提示用户核对
- 即使识别失败，也保留截图作为凭证

#### 成本控制

- 单次调用约 1500-2500 token
- 估算单次成本：¥0.05-0.10
- 用户每次调用前检查 `ai_quota_left > 0`

### 7.2 后续AI功能规划（V1.5+）

按重要性排序：

1. **抄表读数识别**：拍水电表照片识别读数
2. **智能催租建议**：根据租客历史和催租阶段，给出话术
3. **租客沟通话术辅助**：刁难问题的回复建议
4. **合同信息自动提取**：上传合同PDF，自动填充租约信息
5. **租金行情建议**：基于地段和户型，给涨租/降租建议

---

## 八、技术架构与选型

### 8.1 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | Next.js 15 + React 19 | 主流、AI最熟、生态成熟 |
| 样式 | Tailwind CSS + shadcn/ui | 快速开发、风格统一 |
| 后端 | Next.js API Routes | 全栈一体化，简化部署 |
| 数据库 | Supabase (PostgreSQL) | 免费额度大、自带认证、RLS强大 |
| 文件存储 | Supabase Storage | 与数据库集成，易用 |
| 部署 | Vercel | 一键部署、免费、全球CDN |
| AI | Anthropic Claude API | 多模态强、中文好 |
| 推送 | 微信公众号模板消息 | 国内首选，免费 |
| 短信 | 阿里云短信服务 | 验证码用，按量付费 |

### 8.2 应用形态

**第一阶段：PWA**

- Web应用 + 添加到桌面
- 能在浏览器和"App化"两种方式使用
- 开发最快、维护最简单
- 不需要应用商店审核

**第二阶段（V1.5）：微信小程序**

- 当用户量达到一定规模
- 利用微信生态降低使用门槛
- 同一份后端，前端改造

**第三阶段（V2.0）：原生App**

- 仅当用户真的有原生需求时再做
- iOS+Android都做，成本高

### 8.3 系统架构图

```
┌────────────────────────────────────┐
│  用户浏览器/手机（PWA）              │
└───────────────┬────────────────────┘
                │
                ▼
┌────────────────────────────────────┐
│  Vercel（Next.js应用）               │
│  ├ 页面渲染                         │
│  ├ API路由                          │
│  └ 边缘计算（推送、定时任务）         │
└───────┬───────────────────┬────────┘
        │                   │
        ▼                   ▼
┌────────────────┐  ┌────────────────┐
│ Supabase        │  │ Anthropic API   │
│ ├ PostgreSQL   │  │ Claude多模态    │
│ ├ Auth          │  │ 图像识别        │
│ ├ Storage      │  │                │
│ └ Realtime     │  │                │
└────────────────┘  └────────────────┘
```

---

## 九、风险与待验证假设

### 9.1 核心待验证假设

我们的产品建立在一系列假设之上。假设若不成立，产品方向需调整。

| 假设 | 风险等级 | 验证方式 |
|---|---|---|
| 个人房东愿为AI助手付费 | 🔴高 | MVP上线后观察付费转化率 |
| AI识别截图准确率>90% | 🟡中 | 提前用100张样本图片测试 |
| 用户接受PWA形态（非原生） | 🟡中 | 邀请20个真实用户使用 |
| 夫妻协同是真需求 | 🟢低 | 你和太太就是真实样本 |
| ¥19/月定价合理 | 🟡中 | A/B测试不同价位 |
| 中国房东对国内SaaS信任度 | 🔴高 | 早期靠口碑积累 |

### 9.2 主要技术风险

- **AI识别错误率**：识别错一次，用户可能再不信任。需要人机协作设计（AI建议+人工确认）。
- **数据安全合规**：身份证号属于敏感信息，需符合《个人信息保护法》。
- **微信生态依赖**：账单分享重度依赖微信，若微信封禁需备选方案。
- **成本失控**：AI调用成本如果不严格管控，可能反向亏损。

### 9.3 主要业务风险

- **市场已有竞品**：巴乐兔等已经有用户基础。差异化必须真正成立。
- **个人房东付费习惯弱**：纯C端付费需要打磨产品到极致。
- **政策风险**：房地产相关产品可能受到监管影响。

### 9.4 应对策略

- 第一版严格控制范围，快速上线获取反馈
- 用真实用户数据驱动决策，不盲目扩张功能
- 保留产品转向能力（如转B端服务中介公司）

---

## 十、开发路线图

### 10.1 设计阶段（当前）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 问题定义 | 确定要做什么 | ✅ 完成 |
| 需求梳理 | PRD与功能列表 | ✅ 完成 |
| 信息架构 | 实体关系 | ✅ 完成 |
| 数据模型 | 数据库表结构 | ✅ 完成 |
| 用户流程 | 核心操作路径 | ⏳ 下一步 |
| 低保真原型 | 黑白线框图 | ⏳ 待办 |
| 用户画像 | Persona | ⏳ 待办 |
| 高保真UI | 视觉设计稿 | ⏳ 待办 |

### 10.2 开发阶段

| 阶段 | 时长（业余） | 内容 |
|---|---|---|
| 项目搭建 | 1周 | 框架初始化、Supabase接入、登录系统 |
| 核心模块开发 | 6-8周 | 房源/租客/租约/账单 |
| AI功能开发 | 2-3周 | 截图识别 |
| 提醒与协同 | 2周 | 推送、家庭组 |
| 打磨与测试 | 2-3周 | UI细节、Bug修复、真实用户测试 |

**业余时间合计：约 4-5 个月到 V1.0 上线**

### 10.3 上线后阶段

| 阶段 | 时长 | 目标 |
|---|---|---|
| 灰度运营 | 1-2个月 | 100个种子用户 |
| 反馈迭代 | 持续 | 高频小版本更新 |
| 商业化启动 | V1.0+3个月 | 开通付费，验证转化 |
| V2.0规划 | V1.0+6个月 | 根据数据决定下一步 |

---

## 附录A：变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-05-07 | 首版正式发布 |

## 附录B：术语表

- **PRD**：Product Requirements Document，产品需求文档
- **MVP**：Minimum Viable Product，最小可行产品
- **SaaS**：Software as a Service，软件即服务
- **PWA**：Progressive Web App，渐进式网页应用
- **RLS**：Row Level Security，行级安全策略（数据库）
- **CRUD**：Create/Read/Update/Delete，增删改查

---

*文档结束。下一步：用户流程图设计。*
